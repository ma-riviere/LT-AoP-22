# Weight [Adults]

```{r}
#| echo: false
#| output: false

source(here::here("src", "init.R"), echo = FALSE)
```

❖ **Data**

```{r}
#| echo: false

responses <- c("Weight")
predictors <- c("Condition", "Stage")

data <- load_Weight(age = "Adults")

contrasts(data$Condition) <- contr.sum
contrasts(data$Stage) <- contr.sum

Weight_name <- "Mouse Weight *(g)*"

data
```

❖ **Description**

```{r}
#| echo: false
#| output: asis

describe_variables(data)
```

❖ **Correlations**

```{r}
#| echo: false
#| output: asis
#| fig.width: 8

corr_matrix_plot(data, vars = c(c(predictors, "Mouse"), responses))
```


## Weight evolution in time (Adults)

```{r}
#| echo: false

is_temporal <- TRUE

## WARN: Always put the chosen model in first position
models <- list(
  glmmTMB(
    Weight ~ Condition * Stage + ar1(Stage + 0 | Mouse),
    family = Gamma("log"),
    data = data,
    REML = TRUE
  ),
  glmmTMB(
    Weight ~ Condition * Stage + (Stage || Mouse),
    family = Gamma("log"),
    data = data,
    REML = TRUE,
    start = list(beta = c(I(mean(data$Weight)), rep(0, 13)))
  )
)
```

```{r}
#| echo: false
#| output: asis

knitr::knit_child(config$templates$base, quiet = TRUE) |> unlist() |> cat(sep = '\n')
```
